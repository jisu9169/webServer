<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index</title>
</head>

<body>
    <h2>회원 관리 시스템</h2>
    <!-- 로그인에 성공했으면 nick님 환영합니다.-->
    <% if(typeof info !== 'undefined'){%>
    <%=info.nick%> 님 환영합니다.

        <!-- 관리자 모드-->
        <%if(info.id =='admin'){%>
        <a href ="list"><button>회원목록 보기</button></a>
        <%}%>
        <a href="update"><button> 회원정보수정 </button></a>
        <a href="delete"><button> 회원탈퇴 </button></a>
        <a href="logout"><button> 로그아웃 </button></a>
    <%}else{%>
        <a href="login"><button>로그인</button></a>
        <a href="join"><button>회원가입</button></a>
    <%}%>
    
</body>

</html>